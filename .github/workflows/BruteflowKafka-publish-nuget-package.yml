name: Publish Bruteflow.Kafka NuGet Package

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.301
    - name: Install dependencies
      run: dotnet restore "src/Bruteflow.Kafka"
    - name: Build
      run: dotnet build "src/Bruteflow.Kafka" --configuration Release --no-restore
    - name: Test
      run: dotnet test "src/Bruteflow.Kafka" --no-restore --verbosity normal
    - name: Pack
      run: dotnet pack "src/Bruteflow.Kafka" --configuration Release
    - name: Publish
      run: |
        dotnet nuget add source https://nuget.pkg.github.com/xtrmstep/index.json -n github -u xtrmstep -p ${{secrets.NUGET_SERVER_TOKEN}}
        dotnet nuget push "src/Bruteflow.Kafka/bin/Release/Bruteflow.Kafka.2.1.1.nupkg" --source "github" --skip-duplicate --no-symbols true
